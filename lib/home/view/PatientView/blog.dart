import 'package:flutter/material.dart';

class PatientBlog extends StatelessWidget {
  const PatientBlog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
